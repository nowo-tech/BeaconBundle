name: beacon-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: beacon-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - COMPOSER_ROOT_VERSION=1.0.0-dev
    command: ["tail", "-f", "/dev/null"]
    networks:
      - beacon-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  beacon-bundle-network:
    driver: bridge
